import { ScrollShadowRoot, type ScrollShadowRootProps } from "./scroll-shadow"

/* -------------------------------------------------------------------------------------------------
 * Component
 * -----------------------------------------------------------------------------------------------*/
export const ScrollShadow = ScrollShadowRoot

export type ScrollShadow = {
  Props: ScrollShadowRootProps
}

/* -------------------------------------------------------------------------------------------------
 * Variants
 * -----------------------------------------------------------------------------------------------*/
export type { ScrollShadowVariants } from "@heroui/styles"
export { scrollShadowVariants } from "@heroui/styles"
export type { ScrollShadowVisibility } from "./scroll-shadow"
